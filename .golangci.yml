---
linters:
  disable-all: true
  enable:
    - govet
    - staticcheck
    - gocritic
    - gofmt
    - goimports
    - errcheck
    - gosimple
    - unused
    - unconvert
    - goconst
    - gocyclo
    - revive

linters-settings:
  wrapcheck:
    ignoreSigs:
      - .JSON
      - .NewHTTPError
      - .Redirect
      - .NoContent
      - .Errorf(
      - errors.New(
      - errors.Unwrap(
      - .Wrap(
      - .Wrapf(
      - .WithMessage(
      - .WithMessagef(
      - .WithStack(    